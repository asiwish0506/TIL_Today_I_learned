# 수많은 필터조건을 각각 변수로 만들어서, 리스트 안에 모았다. filter_list = [filter1, filter2, filter3...]

# 이걸 데이터프레임에 넣어서 dataframe[filter_list] 와 같이 적용하여
 수백개의 필터를 한꺼번에 적용하려고 하는데,
 반복문 등을 통해 filter1 ~ filter100의 이름을 만들려면 텍스트 데이터로밖에 만들 수가 없어서
 이 텍스트 데이터를 변수로 변환할 필요가 있었다.
 
 가령 'filter1' 이라는 텍스트를, filter1이라는 변수로 변환하는 작업이다.
 
 생각보다 간단하게 가능했다.
 
 globals()['이 안에 텍스트']
 
 
