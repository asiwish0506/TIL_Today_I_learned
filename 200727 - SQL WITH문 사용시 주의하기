WITH문을 작성을 통해 임시 테이블을 만들 때, 여러개를 만드는 경우

WITH t1 AS 
(SELECT ~~)

, t2 AS
(SELECT ~~)

, t3 AS
(SELECT ~~)

와 같이 컴마로 연결하여 작성해야 함.
WITH 구문 자체를 여러개를 쓰는 경우 오류 발생.

t2에서 t1을 활용하고, 바로 또 연이서 t3에서 t2를 활용하는 건 가능!
